on:
  workflow_call: # Called via release.yml

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            suffix: linux-x64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            suffix: windows-x64.exe
          - os: macos-latest
            target: x86_64-apple-darwin
            suffix: macos-x64

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build CLI tool
        run: |
          mkdir -p ./artifact/cli

      - uses: actions/upload-artifact@v4
        with:
          name: cli-artifact-${{ matrix.suffix }}
          path: ./artifact/cli
